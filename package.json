{
 "name": "hostinger-js-template",
 "private": true,
 "scripts": {
  "client:install": "cd client && npm install",
  "server:install": "cd server && npm install",
  "client:build": "cd client && npm run build",
    "go" : "npm run build && npm run start",
  "build": "npm run client:install && npm run client:build && npm run server:install",
  "start": "npm run build && cd server && npm install && npm run start:prod"
 },

 "engines": {
 "node": ">=18"
 }
} 